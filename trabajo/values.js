
    //ejemplo 1
   let car = ['Renault', 'Ford', 'Ferrari', 'Lamborghini', 'Fiat'];

   let iterador = car.values();
   
   for (let value of iterador) {
     let info = `Marca disponible: ${value}`;
     console.log(info);
   }

   //ejemplo 2

 let animal = ['Cat', 'Dog', 'Pig', 'Penguin', 'Tiger', 'Lion'];

   let iterador2 = car.values();
   
   for (let value of iterador2) {
     let info = `Animal encontrado: ${value}`;
     console.log(info);
   }